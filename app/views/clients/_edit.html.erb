<%= form_with model: @client, url: client_path(@client.slug), id: "update_client" do |f| %>

    <%= f.label :name, "Name" %>
    <%= f.text_field :name %><br/>

    <%= f.label :email, "Email" %>
    <%= f.text_field :email %><br/>

    <%= f.submit "Edit" %>

<% end %>
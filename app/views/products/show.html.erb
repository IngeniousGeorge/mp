<%= render 'nav/top' %>

<%= render 'nav/catalogue_toolbar', locals: { categories: @categories } %>

<%= render partial: 'layouts/presenter', locals: { presentee: @seller } %>

<%= render partial: 'products/full', locals: { product: @product, basket_id: @current_basket.id } %>

<%= render 'nav/footer' %>
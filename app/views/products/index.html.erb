<%= render 'nav/top' %>

<%= render 'nav/catalogue_toolbar', locals: { categories: @categories } %>

<% @products.each do |product| %>
  <%= render partial: "products/card", locals: { product: product, basket_id: @current_basket.id } %>
<% end %>

<%= render 'nav/catalogue_pagination' %>

<%= render 'nav/footer' %>
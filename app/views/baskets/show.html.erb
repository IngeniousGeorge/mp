<%= render 'nav/top' %>

Basket
# make it a partial
<% @order = Order.new %>

<%= render partial: 'baskets/edit' %>

<%= link_to t(:place_order), orders_path, order: @order, method: :post %>

<%= render 'nav/footer' %>
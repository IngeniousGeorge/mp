<nav class="navbar navbar-expand-md navbar-light bg-light d-flex align-items-baseline">
  <%= link_to t(:home), root_path, class: "navbar-brand" %>
  <div class="subtitle"><%= t(:subtitle) %></div>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
    <ul class="navbar-nav">
      <li class="nav-item btn btn-outline-primary btn-sm">
        <%= link_to t(:sign_up), new_client_registration_path %>
      </li>
      <% if client_signed_in? %>
        <li class="nav-item btn btn-outline-primary btn-sm">
          <%= link_to t(:log_out), destroy_client_session_path, method: "delete" %>
        </li>
        <li class="nav-item btn btn-outline-primary btn-sm">
          <%= link_to t(:account), client_dashboard_path(current_client) %>
        </li>
      <% else %>
        <li class="nav-item btn btn-outline-primary btn-sm">
          <%= link_to t(:sign_in), new_client_session_path %>
        </li>
      <% end %>
      <% if I18n.locale == :en %>
        <li class="nav-item btn btn-outline-secondary btn-sm">
          <%= link_to "fr", { locale: 'fr' } %>
        </li>
      <% else %>
        <li class="nav-item btn btn-outline-secondary btn-sm">
          <%= link_to "en", { locale: 'en' } %>
        </li>
      <%end%>
    </ul>
  </div>
</nav>